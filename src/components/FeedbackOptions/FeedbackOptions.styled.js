import styled from '@emotion/styled';

export const Button = styled.button`
  margin: 5px;
  font-size: 16px;
  font-weight: 600;
  text-transform: capitalize;
  :active {
    background: #569aff;
  }
`;
